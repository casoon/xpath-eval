[package]
name = "xpath-eval"
version = "0.2.2"
edition = "2024"
description = "A pure-Rust XPath 1.0 expression evaluator, generic over the document model"
license = "MIT"
repository = "https://github.com/casoon/xpath-eval"

[dependencies]
